@model IEnumerable<ImoveisApi.Models.Proprietario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Proprietario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CPF_Proprietario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone_Proprietario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Proprietario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CPF_Proprietario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone_Proprietario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Proprietario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Proprietario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Proprietario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
