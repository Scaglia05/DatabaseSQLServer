@model IEnumerable<ImoveisApi.Models.Contrato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data_Inicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data_Fim)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data_Inicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data_Fim)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Contrato">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Contrato">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Contrato">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
