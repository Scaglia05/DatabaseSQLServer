@model IEnumerable<ImoveisApi.Models.Pagamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data_Pagamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor_Pagamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data_Pagamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor_Pagamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Pagamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Pagamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Pagamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
